name: dependabot
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: langston-barrett/gha/.github/workflows/merge-dependabot-prs.yml@5a8597a5868afd42ebfddeb31c1a6c99f4cba315
    with:
      only-patch: false
