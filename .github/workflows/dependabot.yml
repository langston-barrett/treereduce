name: dependabot
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: langston-barrett/gha/.github/workflows/merge-dependabot-prs.yml@a35f858a9bcd26fc0c80928ac8b0a85c0f791c06
    with:
      only-patch: false
